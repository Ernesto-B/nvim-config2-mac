return {
    "wakatime/vim-wakatime",
    event = "VeryLazy", -- or load it normally if you prefer
    config = function()
        -- no special configuration is needed by default.
        -- just ensure your wakatime_api_key is set in your environment.
    end,
}
